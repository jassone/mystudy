## 一些概念

### 副本

**没有固定的"主库"概念**，所有副本地位平等！

副本A (Replica 1) <──双向同步──> 副本B (Replica 2)
      ↑                    ↑
      └──── 写入任意副本 ───┘

### 虚拟列

比如 **SELECT** x + y **FROM** t_null， x+y就是虚拟列，尽量避免虚拟列，而是改为将虚拟列落盘为一个字段，性能更佳。