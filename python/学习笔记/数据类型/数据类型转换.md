## main

### 隐式转换

- 类型不是固定的，会根据变量当前值在运行时决定

- 运算时，数据类型会向高精度自动转换

  ```
  var1 = 10
  var1 = var1 + 1.2  //  11.2 
  ```

  

- int，float都可以转成str，既用str(x)函数

- int转换为float，会增加小数部分，float转换为int，会去掉小数部分

- 将str类型转成基本数据类型时，要确保str值能够转成有效的数据，比如int("12.3")会报错。



