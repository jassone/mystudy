## for

```
for <变量> in <范围/序列>：
    循环语句


for i in enumerate(m.items()):
    print(i)
    
    // (0,("name","friends"))
```

- 范围/序列是可迭代对象，比如字符串，列表等

## range(start,stop,step)

-  range（10）  == range(0,10,1) 

## while 

```
while <条件>:
  //



while <条件>:
  //
  // 这里如果有break，则不会执行else
else:
  //
```







