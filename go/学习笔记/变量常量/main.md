## 变量常量

### 1、声明

const（声明常量），var（声明变量）, type（声明类型） ,func（声明函数）。

### 2、 类型推断

Go 语言的类型推断只应用在了对变量或常量的初始化方面。

### 3、 nil -todo

是预定义的标识符，代表指针、通道、函数、接口、映射或切片类型的零值。直接使用 `nil == nil` 进行比较会导致编译错误，因为 Go 语言没有定义 `nil` 的比较规则。