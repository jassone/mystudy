## 并发安全问题

todo

**slice并不是并发安全的**

这里保证 slice 线程安全的方法是用互斥锁，**也可以考虑把数据写入、更新的代码封装到一个 channel 中，有一个专门的协程来单独维护 slice 的数据更新**。如：

```text
for {
    data := <- chanList
    list = append(list, data)
}
```

https://blog.csdn.net/2401_85236365/article/details/140569398